# Build output
dist

# Dependencies
node_modules

# The ~1,900-line App lives in main.tsx — dense, hand-tuned code (legacy style:
# semicolons + 4-space indent) intentionally NOT auto-formatted: reflowing it would
# bury its history in a giant diff for no real gain. The extracted modules
# (src/lib, src/components, src/engine, src/store) + tests + configs ARE formatted.
src/main.tsx

# Generated / vendored
package-lock.json

# Hand-tuned / non-JS files we don't want Prettier churning:
# - index.css: dense, deliberately-compact theme CSS (5 palettes on single lines)
#   + the layout architecture; reflowing it hurts readability. Kept hand-formatted.
# - index.html: small, stable shell.
# - deploy.yml: CI workflow, managed by hand.
# Prettier's scope is the JS/JSX app modules (src/lib, src/components, src/store),
# where consistent auto-formatting is the actual win.
src/index.css
index.html
.github/
